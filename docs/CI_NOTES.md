# CI notes

GitHub Actions in this public repository validates only the workspace infrastructure: policy checks, pinned upstream fetches, and container/toolbox builds. Live CTF challenge inputs, outputs, candidate flags, credentials, exploit transcripts, and private notes must never be uploaded to Actions logs, artifacts, issues, pull requests, or tracked files.
